# Memory/

Long-term memory storage for **MEMOS v3**.

## Structure
- `MEMORY.md` — pointer-first master memory overview
- `MEMORY_INBOX.md` — pre-distillation intake queue
- `State/` — current domain state files
- `Log/` — append-only milestone log
- `Patterns/` — learned patterns and reusable cases
- `review-log.md` — audit trail of distillation decisions

## Notes
- Keep active tasks out of `MEMORY.md`.
- Use `Tickets/` for execution state.
- Load `State/` selectively using Relevance Selection.
